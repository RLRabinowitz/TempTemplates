version: 1

deploy:
  steps:
    setupVariables:
      after:
        - npm i
        - echo $HOME is my home
        - echo kaka
    pulumiLogin:
      before:
        - pulumi version
destroy:
  steps:
    setupVariables:
      after:
        - npm i
        - ls
        - ls /opt
        - ls /
        - ls .
        - pwd
