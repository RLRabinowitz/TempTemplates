name: Test
on:
  schedule:
    - cron:  '3 * * * *'
jobs:
  something:
    name: Something
    runs-on: ubuntu-16.04
    timeout-minutes: 30
    steps:
      - name: Somethinggg
        run: echo ${{toJson(github)}}
      
