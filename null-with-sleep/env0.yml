version: 1

deploy:
  steps:
    setupVariables:
      after:
        - rm -rf /opt/pulumi
        - curl -fsSL https://get.pulumi.com | sh -s -- --version 3.33.1
        - mv $HOME/.pulumi/bin /opt/pulumi
        - /opt/pulumi/pulumi version