version: 1

deploy:
  steps:
    setupVariables:
      after:
        - npm i
        - rm -rf /opt/pulumi
        - echo $HOME is my home
pulumiLogin:
      before:
          pulumi version
destroy:
  steps:
    setupVariables:
      after:
        - npm i
        - ls
        - ls /opt
        - ls /
        - ls .
        - pwd
