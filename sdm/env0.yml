version: 1

deploy:
  steps:
    setupVariables:
      after: 
        - curl -J -O -L https://app.strongdm.com/releases/cli/linux
        - unzip sdmcli*.zip
        - ls -la
        - cat ./sdm
        - chmod 777 ./sdm
        - ./sdm
