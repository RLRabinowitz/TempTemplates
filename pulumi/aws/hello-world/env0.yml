version: 1

deploy:
  steps:
    setupVariables:
      after:
        - npm i
        - rm -rf /opt/pulumi
        - echo $HOME is my home

destroy:
  steps:
    setupVariables:
      after:
        - npm i
        - ls
        - ls /opt
        - ls /
        - ls .
        - pwd