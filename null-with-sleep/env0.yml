version: 1

deploy:
  steps:
    setupVariables:
      after: 
        - echo "Good morning!!"

    terraformInit:
      before:
        - /opt/tfenv/bin/terraform workspace select $ENV0_WORKSPACE_NAME
