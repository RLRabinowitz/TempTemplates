version: 1

deploy:
  steps:
    setupVariables:
      after:
        - npm i
        - echo $HOME is my home
    pulumiVersion:
      before:
        - echo "hara"
    pulumiLogin:
      before:
        - pulumi version
destroy:
  steps:
    setupVariables:
      after:
        - npm i
        - ls
        - ls /opt
        - ls /
        - ls .
        - pwd
