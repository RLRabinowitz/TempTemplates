version: 1

deploy:
  steps:
    setupVariables:
      after: 
        - echo "Good morning!!"

destroy:
  steps:
    setupVariables:
      after: 
        - if [[ "$ENV0_WORKSPACE_NAME" == *4 ]]; then failz; fi
