version: 1

deploy:
  steps:
    setupVariables:
      after:
        - which pulumi
        - rm -rf /opt/pulumi
        - ls /home
        - echo $HOME
        - env
        - ls $HOME
        - echo "Good morning!"
